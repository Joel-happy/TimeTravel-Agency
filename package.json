{
  "name": "timetravel-agency",
  "version": "1.0.0",
  "private": true,
  "description": "TimeTravel Agency — webapp interactive (landing + chatbot IA Mistral) déployée sur Vercel.",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev"
  },
  "engines": {
    "node": ">=18"
  }
}
